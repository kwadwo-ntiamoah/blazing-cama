using Data.ViewModels;
using Data;

namespace Services {
    public class AuthService {

        public AuthService(Parameters)
        {
            
        }

        public async Task<AppResponse<Tokens>> Login(LoginViewModel model) {
            var response = new AppResponse

            try {

            } catch (e) {

            }
        }
    }
}